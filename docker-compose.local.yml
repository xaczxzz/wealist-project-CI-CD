services:
  #--- Frontend 개발 환경 설정 ---
  frontend-service:
    build:
      context: ./frontend
      # 💡 개발용 Dockerfile 지정
      dockerfile: Dockerfile.local
    container_name: wealist-frontend-dev
    ports:
      - "3000:5173" # 호스트:컨테이너 (Vite HMR 포트)
    env_file: .env
    volumes:
      - ./frontend:/app
      - /app/node_modules # 호스트 오염 방지용 익명 볼륨
    # 💡 개발 서버 실행 명령 (HMR 활성화)
    command: pnpm dev --host 0.0.0.0 --port 5173 --strictPort
    networks:
      - wealist-net
    depends_on:
      - board-service
      - user-service
    restart: unless-stopped
# Base 파일에 정의된 네트워크와 볼륨을 참조하기 위한 설정
# networks:
#   wealist-net:
#     external: true

# volumes:
#   grafana-storage:
#     driver: local
#   grafana-logs:
#     driver: local
#   prometheus-data:
#     driver: local
