# 사용법:
#   1. 이 파일을 복사: cp docker/env/.env.dev.example docker/env/.env.dev
#   2. 필요한 값 수정 (특히 OAuth 관련 값)
#   3. 절대 Git에 커밋하지 마세요!
# =============================================================================

# =============================================================================
# Application Configuration
# =============================================================================
APP_NAME=wealist-api-dev
VERSION=dev
ENVIRONMENT=development

# =============================================================================
# Database Configuration - PostgreSQL
# =============================================================================
# 개발 환경에서는 간단한 패스워드 사용 가능
POSTGRES_SUPERUSER=postgres
POSTGRES_SUPERUSER_PASSWORD=postgres_dev_pass

USER_DB_NAME=wealist_user_db
USER_DB_USER=wealist_user
USER_DB_PASSWORD=user_dev_pass

BOARD_DB_NAME=wealist_board_db
BOARD_DB_USER=board_service
BOARD_DB_PASSWORD=board_dev_pass

# 개발 환경에서는 호스트에서 접근 가능하도록 포트 노출
POSTGRES_HOST_PORT=5432

# =============================================================================
# Cache Configuration - Redis
# =============================================================================
REDIS_PASSWORD=redis_dev_pass
REDIS_HOST_PORT=6379

# =============================================================================
# JWT Configuration
# =============================================================================
# 개발 환경에서는 고정된 시크릿 사용 (팀원 간 토큰 공유 가능)
JWT_SECRET=dev_jwt_secret_key_for_local_development_only_do_not_use_in_production
JWT_ACCESS_TOKEN_EXPIRATION_MS=86400000      # Access Token: 24시간 (개발 편의성)
JWT_REFRESH_TOKEN_EXPIRATION_MS=604800000   # Refresh Token: 7일

# Board Service에서도 JWT_SECRET을 SECRET_KEY로 참조
# (docker-compose.yml에서 SECRET_KEY=${JWT_SECRET}로 매핑됨)

# =============================================================================
# JPA/Hibernate Configuration
# =============================================================================
JPA_DDL_AUTO=update            # 개발 중 스키마 자동 업데이트
JPA_SHOW_SQL=true              # SQL 쿼리 로깅
JPA_FORMAT_SQL=true            # SQL 포맷팅
JPA_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# =============================================================================
# Service Ports
# =============================================================================
USER_HOST_PORT=8080
BOARD_HOST_PORT=8000
BOARD_SERVER_PORT=8000

# Board Service - User Service 연동 URL
USER_SERVICE_URL=http://user-service:8080

FRONTEND_HOST_PORT=3000

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=debug                # 개발 환경에서는 상세 로깅

# =============================================================================
# OAuth2 Configuration - Google
# =============================================================================
# ⚠️ 개발 환경용 OAuth 클라이언트를 별도로 생성하세요
# Redirect URI: http://localhost:3000/oauth/callback
GOOGLE_CLIENT_ID=your-dev-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-dev-google-client-secret
OAUTH2_REDIRECT_URL=http://localhost:3000/oauth/callback

# =============================================================================
# CORS Configuration
# =============================================================================
# 개발 환경에서는 localhost 전체 허용
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8080,http://127.0.0.1:3000

# =============================================================================
# Frontend Environment Variables
# =============================================================================
VITE_REACT_APP_JAVA_API_URL=http://localhost:8080
VITE_REACT_APP_GO_API_URL=http://localhost:8000
VITE_GOOGLE_CLIENT_ID=your-dev-google-client-id.apps.googleusercontent.com

# =============================================================================
# Monitoring Configuration
# =============================================================================
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin   # 개발 환경에서는 간단한 비밀번호

REDIS_EXPORTER_PORT=9121
POSTGRES_EXPORTER_PORT=9187
NODE_EXPORTER_PORT=9100

# =============================================================================
# Build Configuration
# =============================================================================
CACHEBUST=1
