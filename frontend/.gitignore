# ---------------------------------------
# 1. 의존성 (Dependencies)
# ---------------------------------------
# 패키지 매니저가 설치하는 모든 의존성 제외
/node_modules
# pnpm 사용 시 생성되는 스토어 파일 (pnpm workspace 환경에서)
.pnpm-store
# pnpm 캐시 및 데이터 파일
.pnpm-state.json

# ---------------------------------------
# 2. 빌드 아티팩트 (Build Artifacts)
# ---------------------------------------
# Vite의 기본 프로덕션 빌드 폴더 (JS 청크, CSS, HTML 포함)
/dist
/build

# 번들 분석 도구 (rollup-plugin-visualizer)가 생성하는 HTML 리포트
# 이 리포트는 빌드 결과이므로 Git에 커밋하지 않습니다.
/dist/stats.html

# ---------------------------------------
# 3. 환경 변수 및 보안 파일 (Vite 기준)
# ---------------------------------------
# API 키 등 민감 정보가 포함될 수 있는 파일은 반드시 제외
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# ---------------------------------------
# 4. IDE 및 로컬 캐시 파일
# ---------------------------------------
# VS Code 설정 파일 제외
.vscode/

# 운영체제에서 생성하는 메타데이터
.DS_Store

# ---------------------------------------
# 5. TypeScript 관련 파일
# ---------------------------------------
# TypeScript가 빌드 정보를 캐싱하는 파일 제외
*.tsbuildinfo

# ---------------------------------------
# 6. 테스트 및 Coverage 결과
# ---------------------------------------
/coverage
```eof

### ✅ 핵심 추가 사항