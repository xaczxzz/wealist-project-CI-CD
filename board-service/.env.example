# ⚠️ 이 파일은 참고용입니다.
# 실제 환경변수는 최상위 wealist-project/.env에서 관리됩니다.

# Board Service
ENV=dev
SERVER_PORT=8000
USE_AUTO_MIGRATE=true
LOG_LEVEL=debug

# Database (docker-compose에서 주입)
DATABASE_URL=postgresql://board_service:board_pass12345@postgres:5432/wealist_board_db?sslmode=disable

# Redis (docker-compose에서 주입)
REDIS_URL=redis://:pass12345@redis:6379/1

# JWT (docker-compose에서 주입)
SECRET_KEY=CHANGE_THIS_JWT_SECRET_KEY

# User Service (docker-compose에서 주입)
USER_SERVICE_URL=http://user-service:8080

# CORS (docker-compose에서 주입)
CORS_ORIGINS=http://localhost:3000
