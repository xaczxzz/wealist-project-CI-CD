name: Dev Infrastructure Apply (Terraform)

on:
  push:
    branches: [main]
    paths:
      - "terraform/dev/**" # terraform/dev 폴더 내 파일 변경 시에만 실행
      - ".github/workflows/cd/infra-dev-apply.yml" # 워크플로우 파일 변경 시 실행

jobs:
  terraform_apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    environment: development # Secrets 관리를 위해 Development 환경 사용

    defaults:
      run:
        working-directory: ./terraform/dev # Terraform 코드가 있는 디렉토리

    steps:
      - name: ⬇️ Checkout Monorepo Code
        uses: actions/checkout@v4

      # 1. AWS Credentials 설정
      - name: 🔑 Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      # 2. Terraform 설치
      - name: 🛠️ Setup Terraform CLI
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.0 # 사용 중인 버전을 명시 (최신 버전 사용 권장)

      # 3. Terraform 초기화 (백엔드 설정 포함)
      - name: 🔄 Terraform Init
        id: init
        run: |
          terraform init \
            -backend-config="bucket=wealist-terraform-state-dev" \
            -backend-config="key=dev/wealist.tfstate" \
            -backend-config="region=ap-northeast-2"

      # 4. Terraform Plan 실행
      - name: 🔍 Terraform Plan
        id: plan
        run: terraform plan -input=false -out=tfplan

      # 5. Terraform Apply 실행 (실제 인프라 변경)
      # -plan=tfplan: 이전 단계에서 생성된 계획 파일만 적용 (안정성)
      # -auto-approve: CI/CD 환경에서는 수동 승인 없이 자동으로 적용
      - name: 🚀 Terraform Apply
        id: apply
        if: github.ref == 'refs/heads/main' # main 브랜치에만 실제로 적용
        run: terraform apply -auto-approve tfplan
